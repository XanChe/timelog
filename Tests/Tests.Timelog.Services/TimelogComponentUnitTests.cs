using System;
using Xunit;
using Microsoft.EntityFrameworkCore;

namespace Timelog.Services
{
    public class TimelogComponentUnitTests
    {
        //private TimelogComponent timelogService;
        public TimelogComponentUnitTests()
        {
            //timelogService = new TimelogComponent(options => options.UseNpgsql("Host=localhost;Port=5432;Database=test_timelog_db2;Username=tester;Password=formatmeplz"));
        }

        //[Fact]
        //public void TimelogComponentCreateTest()
        //{
        //    Assert.NotNull(timelogService);

        //}

        //[Fact]
        //public void UserActivitiesManagerCreatorTest()
        //{
        //    UserActivityManager userActivityManager = timelogService.CreateUserActivityManager(Guid.NewGuid().ToString());
        //}
    }
}
